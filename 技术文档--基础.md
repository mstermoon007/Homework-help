# 数学题目与练习生成引擎 — 技术文档（基础）

> **引擎版本**: **V2.1（v2.1.0）** — 版本常量见 [shared/generator/generators/index.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generators/index.js) `ENGINE_VERSION`
> **文档定位**: 数学生成引擎的**基础技术文档**（架构 / 模块 / 契约 / 规范 / 门禁 / 决策 / 踩坑）；设计计划与 UI 层设计见 [设计文档.md](file:///Users/zhanggaozhang/Code/Homework%20Help/设计文档.md)；版本演进流水见 [docs/DEV_LOG.md](file:///Users/zhanggaozhang/Code/Homework%20Help/docs/DEV_LOG.md)
> **更新时间**: 2026-09-08（文档体系收敛：原 DEVELOPMENT / ARCHITECTURE_LAYERS / PROJECT_STRUCTURE / KNOWLEDGE_BANK_CONTRACT / REFACTOR_BASELINE 等已并入本文）
>
> **V2.1 质量基线**: ✅ 549/549 KP native 全覆盖 ｜ ✅ 583 QT 生成全通 ｜ ✅ Golden 15/15（错误答案 0）｜ ✅ 274 单测全过 ｜ ✅ 274 JS 文件 0 语法错误 ｜ ✅ Frozen Core 80 文件无漂移 ｜ ✅ lint 0 违规
>
> **版本说明**: `ENGINE_VERSION` 是**数学生成引擎**版本；[shared/version.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/version.js) 的 `APP_VERSION 4.3.0` 是 **PWA 应用/缓存版本**（驱动 Service Worker 更新），两者独立维护。

## 目录

1. [项目概述与技术栈](#1-项目概述与技术栈)
2. [仓库结构与四层架构](#2-仓库结构与四层架构)
3. [系统架构与出题数据流](#3-系统架构与出题数据流)
4. [核心设计：Generator Family 与硬阻断路由](#4-核心设计generator-family-与硬阻断路由)
5. [核心设计：语义解析、生成、渲染与批改](#5-核心设计语义解析生成渲染与批改)
6. [运行时模块与工具 API](#6-运行时模块与工具-api)
7. [知识库契约（KnowledgeBank Contract）](#7-知识库契约knowledgebank-contract)
8. [开发规范与 Frozen Core](#8-开发规范与-frozen-core)
9. [质量保障体系](#9-质量保障体系)
10. [核心设计决策（ADR）](#10-核心设计决策adr)
11. [扩展指南：新增 Generator 标准流程](#11-扩展指南新增-generator-标准流程)
12. [SEO 与发布运维](#12-seo-与发布运维)
13. [版本演进与 V2.1 变更明细](#13-版本演进与-v21-变更明细)
14. [踩坑经验与教训](#14-踩坑经验与教训)
15. [文件索引与验证命令手册](#15-文件索引与验证命令手册)

---

## 1. 项目概述与技术栈

### 1.1 产品目标

一套**零依赖、纯前端、无后端**的小学数学题目与练习生成引擎（Homework Help · 小学练习本）：

- 覆盖小学一至六年级（G1–G6）数学知识体系：基础模块 M0–M12 + 竞赛模块 C1–C9
- 按 **知识点（KP）× 题型（QT）× 难度（d=1..10）** 自动出题
- 即时渲染（HTML 题卡 + 教学图形 SVG）、自动批改、错题重做、A4 打印、离线 PWA
- 支持单知识点练习、多知识点配额练习（知识点驱动生成）、综合练习

> 注：核心生成引擎仅服务数学（math）；语文/英语生成链已在 Core Domain 收缩中移除（非 math 请求返回 `UNSUPPORTED_SUBJECT`，禁止 fallback）。

### 1.2 质量红线

| 指标 | V2.1 标准 |
|---|---|
| 知识点覆盖率 | **549/549 = 100%** 由 native 生成器覆盖，无任何 KP 落到语义错误的生成器 |
| 题型可达性 | **583 个**「KP × 题型」组合全部可成功生成非空题目 |
| 答案正确性 | 每道生成题的标准答案必须通过自身批改（Golden 自测），**错误答案 0** |
| 语义正确性 | 每个 KP 路由到语义匹配的 Generator，禁止泛型兜底静默错接 |
| 架构稳定性 | Frozen Core（80 文件）无未授权漂移；四层归类门禁 PASS |

### 1.3 技术栈

| 类别 | 技术 |
|---|---|
| 前端 | 原生 HTML/CSS/JavaScript（**零第三方依赖**，package.json 无 dependencies/devDependencies） |
| 加载方式 | 经典 `<script>`（全站无 `type="module"`）；`common.js` 按 `document.currentScript.src` 推导目录并 `document.write` 顺序注入同目录子模块 |
| 模块格式 | CommonJS（Node 校验/测试直接 require）；浏览器端经 bundle + 全局挂载双态运行 |
| 随机源 | `crypto.getRandomValues` 优先（`randInt/randFloat/shuffle`），**禁止 `Math.random()`**；种子化 Rng 保证同 seed 同题 |
| 测试 | Node 内置 `node:test`（`test/`、`test/unit/`、`tests/`） |
| CI | GitHub Actions（`.github/workflows/ci.yml`，Node 20，push/PR 跑完整校验套件） |
| 离线 | Service Worker（`sw.js`，经典 Worker + `importScripts('shared/version.js')`） |
| 版本管理 | [shared/version.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/version.js) 单一版本源（APP_VERSION / CACHE_VERSION），`scripts/sync-sw-version.js` 校验 SW 缓存名同步 |
| 构建 | 仅 bundle 打包（`dev/build-strategy-bundle.js` → 70 modules；`dev/build-presentation-bundle.js`），无其他构建步骤 |

---

## 2. 仓库结构与四层架构

### 2.1 物理目录布局（V2.1 现状）

```
Homework Help/
├── index.html              # 首页（双海报营销 + JSON-LD SEO + 科目/年级选择）
├── select.html             # 统一题型选择页（科目/年级/知识点三维选择）
├── subject-types.html      # 转发桩 → select.html（透传 query，保旧链接）
├── math-types.html         # 转发桩 → select.html?subject=math（保留 CollectionPage JSON-LD）
├── practice.html           # 核心练习页（统一宿主：生成/作答/批改/打印/错题本/计时）
├── faq.html / contact.html # 常见问题 / 问题反馈
├── sw.js                   # Service Worker 离线缓存（缓存名随 APP_VERSION）
├── 技术文档--基础.md        # 本文档
├── 设计文档.md              # 设计计划 + UI 层设计说明
├── README.md / llms.txt / robots.txt / sitemap.xml / CNAME
├── shared/                 # 唯一公共来源（single source of truth）+ Frozen Core
│   ├── common.js           # 聚合入口 + PluginUtil（随机/标准化/工厂/renderCard）
│   ├── core.js             # 运行时核心：crypto 随机 / normalizeAns / 布局 / 路由
│   ├── render.js           # 渲染工厂（renderCard / renderGrid / 题目卡布局）
│   ├── check.js            # defaultQCheck 批改（multi 空拆分 / 整串比较）
│   ├── ui-state.js / storage.js / logger.js / metrics.js
│   ├── difficulty.js (+ difficulty-static.js)  # 难度系统（static 为休眠基线）
│   ├── version.js          # APP_VERSION 单一版本源
│   ├── knowledge-bank.js + knowledge-point.js + knowledge-math.js  # 数学知识库（只读）
│   ├── knowledge-ontology*.js / module-catalog.js / capability-*.js  # 本体/模块目录/能力模型
│   ├── subject-utils.js    # MathUtil（rangeByLevel / gcd/lcm / 分数运算 …）
│   ├── generation-engine.js + practice-session.js + practice-bridge.js  # 生成编排/会话/关联层
│   ├── generation/         # orchestrator / api / dto / adapters / services
│   ├── strategy/           # 策略引擎（16 模块，见 §6）
│   ├── generator/          # 路由 + 23 生成器 + core 结构构造（见 §4/§5）
│   ├── presentation/       # render-format / html-renderer / svg-registry / renderer
│   ├── validator/          # 验证管线（M5）
│   ├── learner/            # 学习者模型（M6：掌握度/错因/回灌）
│   ├── schemas/            # 语义题/题型 JSON schema
│   ├── svg-core/calculation/geometry/make-ten/templates.js  # 教学 SVG 生成
│   └── tokens/base/components/states/toolbar/pages/subjects.css  # 分层样式（@layer 锁序）
├── plugins/                # 仅余 6 个教学图形 SVG 插件（clock/area/fraction/data-stats/draw/competition）
├── knowledge/              # 400+ 生成的 SEO 静态知识点页（scripts/generate-knowledge-pages.js）
├── architecture/layers.json# 权威四层机器清单（verify:layers 门禁）
├── dev/                    # 校验/门禁/lint/bundle 脚本（71 个，质量保障）
├── scripts/                # 代码生成/同步（knowledge 页/sitemap/JSON-LD 注入/SW 版本）
├── test/ + tests/          # node:test 单元/契约测试
├── docs/                   # 文档：DEV_LOG.md（开发日志）
├── archive/                # 历史归档（死代码/迁移备份/已应用草案）
└── .github/workflows/ci.yml
```

### 2.2 为什么文件物理驻留 `shared/`（不拆目录）

`shared/` 是紧密耦合的公共模块根，**不存在安全可物理拆分的孤立文件**：

- **common.js 自推导注入**：浏览器端依据 `document.currentScript.src` 推导同目录 base，`document.write` 注入 core/render/check/ui-state/storage 等子模块；拆目录引导即断。
- **bundle 内嵌模块 id**：`strategy-engine.bundle.js` / `presentation-engine.bundle.js` 内嵌 `shared/...` 模块命名空间，移动源文件需重编译并保持 id 稳定。
- **硬编码路径面**：`sw.js`（importScripts + precache）、全部 `dev/*.js` 门禁、`scripts/*.js`、`tests/`、`package.json` 均硬编 `shared/` 路径。

故采用「**物理稳定 + 逻辑清单**」：文件留在 `shared/`，逻辑归属以 [architecture/layers.json](file:///Users/zhanggaozhang/Code/Homework%20Help/architecture/layers.json) 为唯一权威分类；新增 `shared/`（或顶层 js/css）文件必须在 layers.json 登记，否则 `verify:layers` 判未归类。

### 2.3 四层逻辑架构

| 层 | 职责 | 典型路径 | 禁止 |
|---|---|---|---|
| **UI 层** | 显示与交互：页面壳、配置区、题卡宿主、结果展示 | `*.html`、`knowledge/*.html`、`sw.js`（旁路基建） | 不决策题目结构、不拼题卡；唯一生成入口 `PracticeBridge.start` |
| **生成层** | 题目生成核心引擎：策略规划、KP 语义解析、生成器路由与产出、语义归一、HTML/SVG 渲染 | `generation-engine.js`、`practice-session.js`、`strategy/`、`generator/**`、`presentation/**`、`svg-*.js`、`print.js` | **Frozen Core**，仅授权 Bug Fix |
| **知识点库** | 基层数据核心：唯一数据来源、本体 schema、规范映射 | `knowledge-bank.js`、`knowledge-point.js`、`knowledge-math.js`、`knowledge-ontology*.js`、`module-catalog.js`、`question-type-registry.js`、`knowledge/` | 不生成题面、不参与 UI 决策 |
| **大服务层** | 信息传递 / 项目控制 / 外围样式 / 学习者模型 / 校验 | `practice-bridge.js`、`common/core/check/ui-state/storage`、`difficulty*`、`capability-*`、`learner/**`、`validator/**`、`version.js`、`*.css` | 不侵入生成算法 |

### 2.4 三层功能架构（调用方向）

```
UI 层（页面/交互）
   index.html · select.html · practice.html · faq.html · contact.html
        │  只调 PracticeBridge 公开入口（start/submit/newSession/onStartFeedback/onSubmitFeedback）
        ▼
关联层（Bridge）— shared/practice-bridge.js
   PracticeBridge.control（ControlService）集中解析 count/difficulty/knowledgePoint(s)/
   kpAllocation（每-KP 配额），产出执行计划：
     · single：单次 count + knowledgePoints[] 直发生成层
     · orchestrated：多 KP 带配额时按知识点驱动——并发调生成层 start() 后合并为一个题目集，
       聚合会话统一批改；不改生成层即兑现「每知识点数量」
        │  把 UI 意图翻译为生成层配置
        ▼
生成层（Frozen Core）
   PracticeSession · GenerationEngine · StrategyEngine · GeneratorSelector/Generators ·
   Validator · PresentationRenderer
   只消费：count/difficulty/knowledgePointId(s)/questionType/adaptive …
```

### 2.5 分层硬红线

1. **生成层 = Frozen Core**：禁止重构/重设计/新增核心抽象，仅授权 Bug Fix（流程见 §8.4）。
2. **UI 层唯一生成入口 = `PracticeBridge.start`**（`dev/check-ui-boundary.js` / `check-practice-page.js` 门禁锚定）；UI 不直接 `new PracticeSession`、不直接调 `StrategyEngine.plan()`/`Generator.generate()`。
3. **知识库不生成题面、不参与 UI 决策**（`dev/check-architecture-rules.js` R1–R5）；`knowledge/` 静态页只允许 `.html` 且必须带生成脚本写入的 `kbgen:hash` 标记。
4. **大服务层只做信息传递/控制/外围**，不侵入生成算法。
5. **native 单轨**：`generator-mode.js` 默认 `globalMode='native'`，选择器仅解析 core 候选；legacy 插件轨道已整体退役（V2.0b），**无 legacy fallback**。

### 2.6 三维螺旋·自适应预留契约

| 维度 | 落点 | 状态 |
|---|---|---|
| 难度 | 年级难度锚点表（G1 1-2 … G6 6-10），`difficulty-strategy`/`target-difficulty` clamp 1-10 | ✅ 已落库（`verify:difficulty-anchor` 门禁） |
| 深度转折 | `spiral-strategy`（S1–S6）；深度度量 = `complexityScore(difficulty) + spiral_level + 前置链深度`，随年级严格递增 | ✅ 已校验（`check-spiral-consistency`） |
| 自适应（预留） | `adaptive-strategy` 已实现、**默认关闭**；预留字段 `request.adaptive` / `learnerProfile` / `adaptiveDelta`（±2，已 clamp），未消费时安全忽略 | 🔒 固化契约，不做新开发 |

---

## 3. 系统架构与出题数据流

### 3.1 分层总览

```
┌─────────────────────────────────────────────────────────────────┐
│ 页面层   practice.html / select.html / index.html                │
│          （URL 参数解析、答题收集、批改反馈 UI、打印）              │
├─────────────────────────────────────────────────────────────────┤
│ 关联层   practice-bridge.js（PracticeBridge + ControlService）    │
├─────────────────────────────────────────────────────────────────┤
│ 表现层   presentation-engine / html-renderer / renderer.js        │
│          render-format.js（SemanticQuestion→可渲染格式，唯一轨）   │
│          svg-registry.js（教学图形描述符→SVG）                     │
├─────────────────────────────────────────────────────────────────┤
│ 生成编排 generation-engine / generation/(orchestrator,api)        │
│          strategy-engine（难度→数域/步数/算符约束 QuestionPlan）    │
├─────────────────────────────────────────────────────────────────┤
│ 路由层   generator-selector.js（评分 + 硬阻断链）                  │
│          generator-registry.js（CORE_RECORDS 绑定表）             │
├─────────────────────────────────────────────────────────────────┤
│ 生成层   23 个 Core Generator（generators/*.js）→ SemanticQuestion │
│          core/arithmetic-core.js（算术结构构造）                   │
│          core/kp-arithmetic-semantics.js（算术语义解析）           │
│          core/kp-complex-semantics.js（复杂运算语义解析）          │
├─────────────────────────────────────────────────────────────────┤
│ 知识层   knowledge-bank.js / knowledge-math.js（549 KP，只读）     │
│          knowledge-point.js（KP.get() 规范对象 + 惰性索引缓存）     │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 一次出题的数据流

```
用户请求 (kp/qt/难度/题量)
  → PracticeBridge.control 解析为执行计划（single / orchestrated）
  → StrategyEngine 生成 QuestionPlan
      · GeneratorSelector.selectGenerator(plan) 路由到 Generator（评分+硬阻断）
      · KP 语义解析（arithSem / complexSem）注入 plan.constraints
        （operation 算符集 / exactSteps 精确步数 / kind 专用结构 / structure.family）
  → Generator.generate(plan, context)
      · 种子化 Rng（seed = kp|family|难度|count）
      · 按 kind/family 分派结构构造器（算术核心 / 复杂运算 / 专项模板）
      · 输出 SemanticQuestion[]（prompt + answer{value,acceptable} + data 结构）
  → RenderFormat.toRenderableQuestions() 转为可渲染格式（inputType/options/多空输入）
  → html-renderer 渲染题卡；graphic 描述符经 SVGRegistry 出图
  → 用户作答 → check.js defaultQCheck（normalizeAns 单源归一化）→ 批改结果回灌 learner
```

### 3.3 双态运行与构建纪律

- **源文件**：`shared/` 下 CommonJS 模块；浏览器端 `core.js` 等先建立全局对象，跨模块经全局解析。
- **Bundle**：`strategy-engine.bundle.js`（70 modules）由 `dev/build-strategy-bundle.js` 打包，`presentation-engine.bundle.js` 同理。
- **纪律**：**改动 `shared/` 源文件后必须 `npm run build:strategy`（动表现层再 `build:presentation`）重建**，否则浏览器跑的是旧 bundle（V2.1 排查竖式问题时实际踩中，见 §14-P7）。
- 批改/渲染工具：`normalizeAns` / `defaultQCheck` 由 core.js/check.js 挂全局（`global.normalizeAns`、`PluginUtil.*`），跨模块裸调用。

---

## 4. 核心设计：Generator Family 与硬阻断路由

### 4.1 23 个 Core Generator 总表

| Family | Generator ID | 能力题型 | 绑定 KP（V2.1 实测） |
|---|---|---|---|
| Arithmetic | `arithmetic-addition` | oral/calc/fill/apply | 33 |
| Arithmetic | `arithmetic-subtraction` | oral/calc/fill/apply | 1 |
| Arithmetic | `arithmetic-multiplication` | oral/calc/fill/apply | 14 |
| Arithmetic | `arithmetic-division` | oral/calc/fill/apply | 18 |
| Arithmetic | `arithmetic-mixed-calculation` | oral/calc/fill/apply | 0（能力占位） |
| Selection | `selection-fill` | fill/recognize/calc/oral/apply | 42 |
| Selection | `selection-choice` | choice/recognize/calc/oral/apply | 12 |
| Selection | `selection-judge` | judge/recognize/calc/oral/apply | 3 |
| Complex | `complex-calc` | calc/fill/oral | 12 |
| Shape | `shape-recognition` | choice/judge/fill/oral/geometry/recognize/apply | 104 |
| Position | `position-direction` | choice/judge/fill/oral | 8 |
| Money | `money-measurement` | fill/choice/judge/apply/calc/oral | 4 |
| Application | `application-word` | apply/fill/choice/judge/calc/oral/open | 92（纯标准应用题） |
| Composite | `composite` | calc/judge/fill/apply/oral | —（复合题编排） |
| Counting | `counting` | apply/calc | 29 |
| Reasoning | `reasoning` | apply/calc | 22 |
| Stats | `stats` | apply/calc | 21 |
| Picture-Equation | `picture-equation` | apply/calc | 19 |
| C1 竞赛 | `c1-number-puzzle` | apply/calc | 12 |
| C2 竞赛 | `c2-number-theory` | apply/calc | 23 |
| C5-C6 竞赛 | `c5-c6-journey-engineering` | apply/calc | 27 |
| C7 竞赛 | `c7-clever-calc` | apply/calc | 19 |
| C9 竞赛 | `c9-comprehensive` | apply/calc/open | 34 |

> 实测分布合计 = 549（每个 KP 按其题型路由命中的 priority 生成器归并计数）。

### 4.2 为什么需要硬阻断路由

V1 时期只有「能力匹配 + 优先级评分」，没有语义隔离，导致：

- `arithmetic-addition` 接到图形几何 KP；`arithmetic-division` 接到 C2 竞赛数论 KP
- `application-word` 作为「能力最全」的兜底生成器，接到 342 个非应用题（其中 110 个语义错误）
- G2 竖式 KP 无显式绑定，减法竖式回退到加法生成器、余数/连算竖式回退到应用题生成器

**结论：泛型兜底 + 无显式绑定 = 静默错接。** V2 的路由体系由此建立。

### 4.3 路由评分与硬阻断链

每个 Generator Family 在 [generator-selector.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generator-selector.js) 同步**四处**判定：

```js
// ① isXxxFamily(g) — 生成器 id 归属
function isArithmeticFamily(g) {
  return g.id.indexOf('generator:arithmetic-') === 0 ||
         g.id.indexOf('generator:selection-') === 0;
}

// ② hasXxxSemantics(kp) — KP 语义判定（pluginId / graphicType / moduleId）
function hasShapeSemantics(kp) {
  if (!kp) return false;
  if (kp.graphicType === 'geometry') return true;
  if (kp.pluginId && (kp.pluginId.indexOf('geometry') !== -1 ||
                      kp.pluginId.indexOf('area') !== -1)) return true;
  return false;
}

// ③ 硬阻断链：score.kp===0（未显式绑定）且语义不符 → 直接 reject
var qt = plan.questionTypeId;
var isRecognize = qt === 'recognize';   // recognize 元题型豁免所有阻断
if (!isRecognize && isArithmeticFamily(g) && score.kp === 0 &&
    !(arithSem || isAlgebraDomain || (kp && kp.operations && kp.operations.length))) return;
if (!isRecognize && isComplexFamily(g)   && score.kp === 0 && !complexSem) return;
if (!isRecognize && isShapeFamily(g)     && score.kp === 0 && !hasShapeSemantics(kp)) return;
// … 每个 family 一行 …
if (!isRecognize && isC9Family(g)        && score.kp === 0 && !hasC9Semantics(kp)) return;

// ④ semanticOp 加分：family 匹配的生成器额外 +1 分，保证专用 > 泛型
if (isArithmeticFamily(g) || /* … */ || isC9Family(g)) score.semanticOp = 1;
```

### 4.4 硬阻断语义判定全景

| Family | 放行语义 | 阻断效果 |
|---|---|---|
| Arithmetic | `arithSem`（算术语义解析通过）或代数域或 `operations.length>0` | 非算术 KP 被泛型算术生成器接 → 拒绝 |
| Complex | `complexSem`（复杂运算语义解析通过）或 pluginId 含 `complex` | 非复杂运算被泛型接 → 拒绝 |
| Shape | `graphicType=geometry` 或 pluginId 含 `geometry`/`area` | 算术 KP 被图形生成器接 → 拒绝 |
| Money | pluginId 含 `money`/`time`/`unit`/`measure` | 非钱/度量被泛型接 → 拒绝 |
| Counting | pluginId 含 `combination`/`counting` 或 C3 竞赛 | 非计数被泛型接 → 拒绝 |
| Reasoning | pluginId 含 `logic`/`reason`/`c8` 或 C8 竞赛 | 非逻辑推理被泛型接 → 拒绝 |
| Stats | `graphicType=chart` 或 pluginId 含 `stats`/`data` | 非统计被泛型接 → 拒绝 |
| Picture-Equation | `graphicType=diagram` 或 pluginId 含 `picture` | 非看图列式被泛型接 → 拒绝 |
| C1 / C2 / C5-C6 / C7 / C9 | pluginId 同时含 `c1/c2/c5|c6/c7/c9` 与 `competition`（C9 亦认 moduleId=C9） | 非该竞赛域被泛型接 → 拒绝 |
| **recognize 题型** | **豁免全部硬阻断** | recognize 是元题型（识别/判断/区分），不属于任何单一语义域 |

### 4.5 语义归类优先级

当一个 KP 同时满足多个 family 的语义判定时，以**更强的结构性信号优先**：

- `graphicType=diagram`（数阵/幻方的看图填数）优先于 `pluginId 含 c1` → 数阵/幻方归 picture-equation 而非 c1-number-puzzle
- 图形几何计数（需在图中数三角形/矩形）归 shape-recognition 而非 counting（counting 无图形能力）
- recognize 类的时钟/比较大小 KP 留 selection-fill，不进 apply/calc 专用竞赛生成器

---

## 5. 核心设计：语义解析、生成、渲染与批改

### 5.1 KP 语义解析层

路由层只回答「哪个生成器有资格」，语义解析层回答「这个生成器应该产出什么结构」，两者解耦：

| 模块 | 职责 | 输入 → 输出 |
|---|---|---|
| [kp-arithmetic-semantics.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/core/kp-arithmetic-semantics.js) | 单步纯算术解析 | KP → `{operators, steps, kind?}`；不匹配返回 null |
| [kp-complex-semantics.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/core/kp-complex-semantics.js) | 复杂运算解析（按 KP id 建 profile 表） | KP → `{family: chain/no-bracket/bracket/inverse, operators, steps, allowBracket}` |

算术语义 profile 覆盖：`addsub/add/sub/mult/div/remainder` + 13 种口算专项 kind（凑十、破十、乘整十、小数/分数口算等）；不可迁移类型（`mixed/relation/multi1/twodigit/div1/fraction/decimal/…`）显式列入 NON_MIGRATABLE 返回 null，**宁可不给约束，不可给错约束**。

复杂运算 profile 覆盖 G2-M3 脱式/括号/逆向运算，以及 V2.1 新增的 G2-M2 竖式连算族（`chain-add-col / chain-sub-col / mixed-col`，family=chain，2 步 3 操作数，竖式形态由表现层承载）。

### 5.2 算术结构构造器（arithmetic-core）

[core/arithmetic-core.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/core/arithmetic-core.js) 是唯一的「数字从哪来」出口：

- **15 种 SPECIAL_KINDS 专用结构**：凑十/破十/连减/乘整十/除整十/估算/小数四则/分数口算/**有余数除法** 等
- **通用 generateStructure**：按 constraints（operations/精确步数/数域/结果非负）循环构造 + 种子重试

**有余数除法（V2.1 新增）**：

```js
// a ÷ b = q …… r，恒有 0 < r < b；表内域 b,q∈2..9，a = b·q + r ≤ range.max
function buildDivRemainder(rng, range) { /* 循环采样 b/q/r → a=b*q+r，校验 a≤max */ }
// 输出: { operands:[a,b], operators:['÷'], steps:1, answer:"q……r" }
```

答案直接携带教材标准余数记号 `"q……r"`，结构层保证数学约束（余数小于除数、商为表内一位数）。

### 5.3 复杂运算生成（complex.js）

| family | 结构 | 典型题 |
|---|---|---|
| chain | 2-3 操作数同级运算（+/−/×/÷ 或组合） | 脱式计算、竖式连加连减、加减混合 |
| no-bracket | 两级运算（先乘除后加减），无需括号 | `3 + 4×2` |
| bracket | 显式括号，且括号改变结果顺序 | `(3+4)×2`（构造时校验去括号结果不同） |
| inverse | 结果还原 + 挖空（逆向填数） | `□ × 3 + 5 = 20` |

### 5.4 SemanticQuestion 数据契约

生成器统一输出语义题对象：

```js
{
  knowledgePointId: 'math-g2-m2-remainder-col',
  prompt: '15 ÷ 2 = ?',
  answer: { value: '7……1', acceptable: [] },   // 标准答案 + 可接受变体
  data: { operands:[15,2], operators:['÷'], steps:1 },
  graphic: null | { type:'area-model', ... },   // 教学图形描述符
  difficulty: 5, generatorId: 'generator:arithmetic-division'
}
```

### 5.5 渲染链（V2.1 单轨）

- [render-format.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/presentation/render-format.js)：SemanticQuestion → 可渲染格式（inputType/options/多空 multi 输入），**V2.1 已无 LegacyAdapter，渲染只有这一条路径**
- 教学图形：生成器输出**图形描述符**（graphic descriptor）→ [svg-registry.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/presentation/svg-registry.js) 分派到 6 个保留的 SVG 图形插件（clock/area/fraction/data-stats/draw/competition）与 `shared/svg-*.js` 基础 SVG（几何/竖式/凑十）；不再生成裸 HTML/SVG 字符串
- `presentation-engine.bundle.js` / `strategy-engine.bundle.js`（70 modules）由源文件打包

### 5.6 答案批改与单源归一化

**单一事实源原则**：所有答案比较必须且只能经过 [core.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/core.js) 的 `normalizeAns`：

```js
function normalizeAns(v) {
  return String(v == null ? '' : v)
    .trim().replace(/\s+/g, '')
    .replace(/(…+|\.{2,}|余)/g, '……')   // V2.1：余数记号归一
    .toLowerCase();
}
```

- 余数除法记号 `……` / `…` / `...(≥2点)` / `余` 统一归一为 `……`；单个小数点（如 3.5）不受影响
- 批改链路：[check.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/check.js) `defaultQCheck`（inputType=multi 时按空拆分逐空比较，否则整串比较）← Golden 自测 `buildUserAnswers` ← 浏览器练习页，**三方共用同一 normalizeAns，任何调用方不得各自 trim/替换**
- 效果：标准答案 `4……3` 与学生输入 `4...3` / `4…3` / `4余3` 同源可比

---

## 6. 运行时模块与工具 API

### 6.1 shared/ 运行时模块一览

| 模块 | 职责 |
|---|---|
| [common.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/common.js) | 聚合入口；浏览器端 `document.write` 注入同目录子模块；挂载 `PluginUtil` |
| [core.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/core.js) | crypto 随机源、`normalizeAns`、布局（Layout）、路由 |
| [render.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/render.js) | `renderCard` / `renderGrid` 题目卡渲染工厂 |
| [check.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/check.js) | `defaultQCheck` 批改（multi 空拆分 / 整串比较） |
| ui-state.js / storage.js | 答题状态 / 本地持久化（难度、学习者数据） |
| [print.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/print.js) | 打印路由（PRINT_ROUTES）+ PRINT_QCSS 紧凑打印样式；预览与打印共用同一 HTML |
| difficulty.js / difficulty-static.js | 难度系统：1–10 → 结构五档（steps/allowBracket/allowMultDiv/nestedBrackets）+ EMA 自适应（`ema=ema*0.6+rate*0.4`）；static 为休眠基线（`difficultyParams==null` 守卫） |
| [practice-bridge.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/practice-bridge.js) | **关联层**：`PracticeBridge.start/submit/newSession/onStartFeedback/onSubmitFeedback` + `PracticeBridge.control`（ControlService：count/difficulty/KP/配额解析与编排） |
| [practice-session.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/practice-session.js) | 生成会话（start/submit），只消费 count/difficulty/knowledgePointId(s)/questionType/adaptive |
| [generation-engine.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generation-engine.js) + generation/ | 统一生成入口 + orchestrator/api/dto/adapters/services（multi-kp 配额规划） |
| strategy/（16 模块） | strategy-engine（计划）、comprehensive-strategy（综合分配/混排）、difficulty/target-difficulty、number-range/constraint-builder/structure-constraints、spiral/cognitive/context/complexity、adaptive（默认关）、strategy-{request,resolver,result,validator,error} |
| generator/ | 契约/注册/选择/模式 + generators/（23 生成器）+ core/（结构构造与语义解析）+ graphic-renderer |
| presentation/ | render-format（语义题→可渲染）、html-renderer、svg-registry、renderer、render-options/result |
| validator/ | 验证管线：duplicate（seenKeys 去重）/ batch / quality-scorer 等 |
| learner/（5 模块） | learner-model（KP 级掌握度 EMA、置信度）、result-collector（批改回灌）、learner-storage（持久化）、error-model（错因归一）、practice-result |
| capability-*（model/matrix/resolver/contract/scan-context） | 能力模型：KP → 可支持题型矩阵；resolver 经 bundle 打包可用（每进程一次冷启动全量归一） |
| [module-catalog.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/module-catalog.js) | 模块目录：基础 M0–M12（level:basic）+ 竞赛 C1–C9（level:competition，4–6 年级；C6/C7 仅 5–6 年级） |
| [subject-utils.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/subject-utils.js) | `MathUtil`：rangeByLevel / gcd/lcm/reduce / 分数四则 / filterOperators（除零返回 null） |
| [version.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/version.js) | APP_VERSION / CACHE_VERSION / BUILD_DATE 单一版本源 |

### 6.2 工具 API 速查

**随机数（运行时禁止直接 `Math.random()`）**：

| 函数 | 签名 | 说明 |
|---|---|---|
| `PluginUtil.randInt` | `(min, max) → int` | 闭区间整数；crypto 优先，Math.random 兜底（唯一豁免位置在 randInt 内部） |
| `PluginUtil.shuffle` | `(arr) → arr'` | Fisher-Yates，返回**新数组**；禁止 `sort(() => Math.random()-0.5)` |
| `PluginUtil.rand` | `(arr) → item` | 等概率取一元素 |

概率约定：`randInt(0,1)===0`（50%）、`randInt(1,100)<=p`（p%）。

**难度 API**：`diffLevel(d)`（归一 1–10，非法回退 3）、`diffScale(level)`（`1+(level−3)×0.2`）、`diffMax(base, level)`；生成器统一经 `App.Difficulty.consume(options)` 取得结构参数。

**SVG 统一出口**：`App.SVGRenderer.render(graphic, options)`（[svg-registry.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/presentation/svg-registry.js)）；`graphic` 为结构化描述 `{ type, subtype, params }`。基础 SVG 能力：`SVGGenerators.core`（基础元素/viewBox）、`.math.geometry`（平面立体图形+标注）、`.math.calculation`（四则竖式）、`.math.makeTen`（凑十三法图解）。

---

## 7. 知识库契约（KnowledgeBank Contract）

只读契约层：[knowledge-bank.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/knowledge-bank.js)（数据源）、[knowledge-point.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/knowledge-point.js)（Canonical 访问层）、knowledge-ontology*（归一/校验）、[generator-registry.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generator-registry.js)（生成器绑定权威）。

### 7.1 运行时最低生成契约（必填字段）

| 字段 | 类型/约束 | 运行时消费点 | 缺失后果 |
|---|---|---|---|
| `id` | string 非空，math- 前缀，四段式全局唯一 | 池去重、路由、指纹、QuestionPlan、Rng seed | 无法解析/去重，不可生成 |
| `name` | string 非空 | 题干/标题文案 | 题干残缺 |
| `pluginId` | string（生成器族语义标识） | KP 语义判定（hasXxxSemantics）、operations/errors 映射 | 语义解析降级（生成器绑定以 registry CORE_RECORDS 为权威） |
| `type` | string（教研归类） | 题型/QT 匹配兜底、难度归一 | 题型匹配降级 |
| `weight` | number ≥ 1 | 综合练习加权采样（canonical 回落 1） | 采样平滑降级 |
| `moduleId` | **结构派生**（条目所在 `modules[].moduleId`，短码如 `'M2'`） | teacher/competition 单元池过滤 | 单元池取不到该 KP |

- **ID 四段式**：`math-g{grade}-{moduleIdLower}-{baseSlug}`（全小写+数字+连字符，无空格/下划线）；示例 `math-g1-m1-addsub-20`；slug 跨年级同主题保持一致（如 `g4-c1-c1-vertical` / `g5-c1-c1-vertical`）。
- **数据源只读**：knowledge-bank.js / knowledge-math.js 禁止直接打补丁；生成能力通过 registry 绑定 + 语义函数体现。

### 7.2 可选字段（缺省回落，WARNING 不阻断）

| 字段 | 回落默认 |
|---|---|
| `spiral_level` / `max_spiral_level` | spiral.level=1 / maxLevel=6 |
| `cognitive_level` | cognition.level=0（unknown） |
| `max_steps_default` | `Number()` 取整，<1 回 1 |
| `number_range_default` | 按年级默认 `{min,max}` |
| `context_default` | `defaults=[]`、`allowPure=true` |
| `applicable_question_types` | 由能力矩阵推导（含 `type`/`coefficient`） |
| `operations` / 错误映射 | 经语义治理推导；推导不出为 `[]`/`{}` |

**分级**：缺可选字段 = WARNING（不阻断）；缺必填字段 / 客观非法（`min>max`、`max_steps<1`、`spiral>max_spiral`）= ERROR（阻断）。

### 7.3 数据质量门禁（KP 修改最小门禁）

修改/新增 KP 后跑 **`npm run verify:kb-change`**（契约 → knowledge 目录 → 完整性 → Canonical 审计 → 题型/模块一致性 → 螺旋一致性）；改 `id`/新增 KP 还需 `npm run generate:knowledge` 重新生成静态页。

### 7.4 防重复扫描（已固化）

| 扫描点 | 机制 |
|---|---|
| `KnowledgeBank.getEntries(subject, grade)` | `__entriesCache`（subject\|grade 键） |
| `KnowledgePoint.get(id)` / `findLegacy(id)` | 惰性 `id→legacy` 索引 + `id→canonical` 缓存（一次 O(1)）；`reset()` 供测试重建 |
| `generator-registry` records/forKnowledgePoint/forQuestionType | `_records` / `_kpCache` / `_qtCache` 惰性缓存 |
| 能力注册表冷启动 | 每进程一次全量归一（549 math KP），非逐请求 |

缓存安全前提：entry/canonical 只读复用，`enhanceKp` 注入为幂等浅注入，无请求间状态泄漏。

---

## 8. 开发规范与 Frozen Core

### 8.1 公共来源与 DOM 边界

- **`shared/` 是唯一公共来源**：所有页面引用 `shared/` 下资源；禁止新建根目录级公共脚本/样式；模块间禁止互相 require 内部实现，公共能力只能经 shared/ 共享。
- **题面 HTML 由渲染层产出**：页面（practice.html）只负责 DOM 生命周期（注入 `#problemsArea`、列数自适应、答案收集、批改标记、打印），不自行拼题卡、不决策题目结构。
- **UI 唯一生成入口** = `PracticeBridge.start`（`dev/check-ui-boundary.js` 门禁）。

### 8.2 随机数规范

统一 `PluginUtil.randInt / shuffle / rand`（crypto 优先）；**禁止 `Math.random()` 直调与 `sort(() => Math.random()-0.5)`**（lint R1，唯一豁免在 randInt 内部）。生成器结构随机使用种子化 `core/rng.js`，保证同 seed 同题、测试可复现。

### 8.3 样式与设计令牌

- 唯一变量来源 [tokens.css](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/tokens.css)；层叠顺序由 `@layer` 锁定：**tokens → base → components → states → toolbar → pages → subjects**。
- 颜色/圆角/阴影/渐变一律用 `var(--brand/--ink/--muted/--line/--ok/--bad/…)`；lint R2 扫描硬编码颜色，`/* allow-color */` 行内豁免特殊场景。
- SVG 表现属性（`fill=`/`stroke=`）不支持 `var()`，保留字面量或改写 style 属性。
- 打印样式：print.js 的 PRINT_QCSS 负责去卡片化/间距收紧（紧凑密度），打印时隐藏所有按钮与无关元素，A4 友好。

### 8.4 Frozen Core 保护

**冻结核心**：已验收、生产稳定的生成架构层（M0–M7）。默认冻结，**仅允许 Bug Fix**，禁止重构/重设计/新增同级核心抽象（NewEngine/NewStrategy/NewGeneratorFramework 等）。

- **冻结范围（80 文件）**：M0 基础设施（common/version/核心 css）、M1 本体知识库、M2 生成器契约（generator-{contract,registry,selector,mode}、retry-loop、generators/）、M3 策略引擎、M4 生成器实现（core/、arithmetic/selection/complex）、M5 验证管线、M6 学习者模型、M7 统一渲染/生成/打印（presentation/、generation-engine、print、practice-session、check、svg-*）。
- **基线机制**：[dev/check-frozen-core.js](file:///Users/zhanggaozhang/Code/Homework%20Help/dev/check-frozen-core.js) 自动扫描冻结清单并对内容 hash，基线存 [dev/frozen-core-baseline.json](file:///Users/zhanggaozhang/Code/Homework%20Help/dev/frozen-core-baseline.json)；`npm run verify:frozen-core`（= `--check`）防漂移，`npm test` / CI 均执行。
- **授权 Bug Fix 流程**：Issue（复现/预期 vs 实际）→ 评估可否外层解决 → 最小改动 + 完整测试 → `npm test` + 相关 verify 全绿 → DEV_LOG 记录 → **`node dev/check-frozen-core.js --baseline` 重锚基线**。
- **核心之外扩展**：新生成器进 `shared/generator/generators/` 并注册（见 §11）；新验证规则进 validator/；新打印模板进 print.js PRINT_ROUTES；新 SVG 图形进 plugins/svg-*.js 注册 SVGRegistry。

### 8.5 linter 关键规则（dev/lint-check.js）

R1 禁 `Math.random()`；R2 禁硬编码颜色（令牌变量，SVG fill/stroke、纯白、阴影色除外）；R3 数学生成器必须声明 moduleId 归属；R4 知识点 id 必须有 `math-` 科目前缀。

---

## 9. 质量保障体系

### 9.1 Gate 门禁链（package.json 实测）

| 命令 | 校验内容 | V2.1 结果 |
|---|---|---|
| `npm test` | SW 版本同步 → 对比度 → verify:setup（含知识库校验）→ kb-dir → 题型/模块一致性 → 螺旋一致性 → 难度双测 → SVG → 难度锚点 → lint | exit 0 |
| `npm run verify:m4` | generator 契约测试 + 注册表/核心生成器/模式/bundle/综合管线 | 549/549、583 QT |
| `npm run verify:golden` | 15 case 生成+渲染+**自答案自批改**；错误答案 0 即 ERROR | 15/15 ✅ |
| `npm run verify`（m0） | M0 统一网关（snapshot 为行为回归 REPORT，不阻断） | PASS |
| `npm run verify:syntax` | 全部 JS 文件语法 | 274 文件 0 错误 |
| `npm run verify:m1` | 本体 schema / operation / factual / error / KB 完整性 / Canonical 扫描 | PASS |
| `npm run verify:m2` | M2 统一网关（能力解析/契约） | PASS |
| `npm run verify:m3` | 策略测试 + config + bundle 重建 + plumbing | PASS |
| `npm run verify:rules` / `verify:layers` | 架构护栏 R1–R5 / 四层归类（layers.json） | PASS |
| `npm run verify:ui-boundary` / `verify:practice-page` | UI→Engine 边界 / practice.html 职责 | PASS |
| `npm run verify:frozen-core` | Frozen Core hash 基线（80 文件） | 无变更 |
| `npm run verify:kb-change` | KP 修改最小门禁（§7.3） | 按需 |
| `node --test test/*.test.js test/unit/*.test.js tests/generator/*.test.js tests/strategy/*.test.js` | 单元/契约测试 | 274/274 |

交付前全量链：

```bash
npm run verify:m4 && npm test && npm run verify:golden && npm run verify:syntax && \
npm run verify && npm run verify:m2 && npm run verify:layers && npm run verify:frozen-core
```

### 9.2 按变更范围选择验证（最小门禁矩阵）

| 变更对象 | 最小命令 |
|---|---|
| KP 数据 | `npm run verify:kb-change`（+ `generate:knowledge` 若改 id/新增） |
| 生成器（能力/题型/绑定） | `npm run verify:m4` + `npm run verify:golden` |
| 策略/规划层 | `npm run verify:m3` |
| 动了 frozen 清单文件 | 改后 `node dev/check-frozen-core.js --baseline` 再 `--check` |
| 改了 shared/ 源 | 先 `npm run build:strategy`（动表现层加 `build:presentation`） |
| 跨层/里程碑/PR | 全量链（§9.1） |

### 9.3 Golden 自测协议

[dev/check-golden.js](file:///Users/zhanggaozhang/Code/Homework%20Help/dev/check-golden.js) 对 15 个代表性 case（含 6 个竖式 case）：生成 → RenderFormat 转可渲染 → 用标准答案构造用户答案（`buildUserAnswers`，对象答案 `{q,r}` 拆为多空）→ 经 PresentationEngine.checkAnswers 批改，要求 results 全 correct；失败计入 `errors`（V2.1 起；历史上是 warnings，是 column 挂账的放行口）。

### 9.4 Snapshot 与 CI

- `verify:snapshot` 为**行为回归工具**（非门禁）：结构漂移只 REPORT 不失败；单跑 `node dev/check-snapshot.js` 以退出码反映漂移，供大规模重构前人工比对 `tests/snapshot/snapshot.json`。
- CI：`.github/workflows/ci.yml`（Node 20）push/PR 跑完整校验套件；本地钩子 `git config core.hooksPath scripts/githooks` 启用 pre-commit。

---

## 10. 核心设计决策（ADR）

| # | 决策 | 理由 / 背景 |
|---|---|---|
| D1 | **知识库只读**：不改 knowledge-bank.js，靠生成器显式绑定（KP id 数组）+ 语义函数判定 | KB 是唯一权威数据源；改数据会让路由事实无法单点验证 |
| D2 | **硬阻断优于软兜底**：无显式绑定且语义不符 → reject，而非「能力匹配就放行」 | 软兜底是 342 个 KP 错接应用题的根因；兜底必然在语义边界静默失败 |
| D3 | **显式绑定优先、语义判定兜底、绝不静默错接** | score.kp=0 时才查语义；显式绑定 > 语义 > 阻断 |
| D4 | **答案归一化单源**：normalizeAns 唯一出口，批改/golden/页面三方共用 | math-g2-column 挂账教训：调用方各自 trim/记号替换必然口径漂移 |
| D5 | **答案可由题干复算**：所有应用题数值整除/合理（火车过桥 (车长+桥长) 能被车速整除），题干自带答案 | 不依赖外部答案表，生成与批改闭环可自证 |
| D6 | **分支判定用稳定 id slug，中文判定用完整词** | 「裂项」泛化会误捕「整数裂项」；slug 不冲突 |
| D7 | **Frozen Core 冻结 + hash 基线**：核心路由/策略/渲染文件纳入 80 文件基线 | 核心文件无意漂移是最难排查的回归来源 |
| D8 | **Bundle 构建纪律**：改 shared/ 源 → build:strategy 重建 → 浏览器验证 bundle | 源改了不重建，浏览器验证的就是旧代码 |
| D9 | **legacy 插件轨道整体退役**而非长期双轨 | 双轨使 549 KP 路由事实无法在 native 侧单点验证；~95 死文件持续污染门禁 |
| D10 | **Golden 自测失败即阻断**：错误答案 0 是 Gate 红线，不是 WARNING | 「记为技术债 + 继续放行」让 column 4/5 通过率长期挂账 |
| D11 | **同文件禁止并行编辑**：批量 Edit 同一文件会静默回退，须逐条编辑后 grep 复核 | 本项目实际观察到的工具行为 |
| D12 | **null 防御双层**：硬阻断是主防线，生成器内部元数据函数仍需 null 保护 | 评分上下文/直接调用可能绕过阻断链 |
| D13 | **文档单一事实源**：架构/模块/契约/规范收敛到《技术文档--基础》，设计与 UI 收敛到《设计文档》，流水归 DEV_LOG；已落地草案/快照用后即删 | 多份半过期文档并存会让后来者读到错误现状（本次文档收敛的动因） |

---

## 11. 扩展指南：新增 Generator 标准流程

**7 步必须同步**（漏一步即回归）：

1. **建生成器文件**：`shared/generator/generators/<name>.js`
   - `id: 'generator:<name>'`；`kps: ['math-xxx', ...]`（显式 KP 数组）；`supports(qt)` 题型白名单；`generate(ctx)` 返回 SemanticQuestion[]，题干自带答案。
2. **注册索引**：[generators/index.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generators/index.js) require + `buildAll()` 追加。
3. **注册 CORE_RECORDS**：[generator-registry.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generator-registry.js) 新增 `{ id, name, knowledgePoints, scope:'core' }`。
   - ⚠️ **双绑定同步**：注册表 CORE_RECORDS 与生成器实例的 `kps` 数组是两处，必须同时改。
4. **selector 四件套**（[generator-selector.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generator-selector.js)）：`isXxxFamily(g)`、`hasXxxSemantics(kp)`（pluginId 全词匹配，必要时辅以 moduleId）、硬阻断行、semanticOp 加分。
5. **语义 profile（如需结构约束）**：在 kp-complex-semantics.js / kp-arithmetic-semantics.js 加 profile；新算术结构进 arithmetic-core.js 的 SPECIAL_KINDS。
6. **更新契约测试**：`tests/generator/kp-binding.test.js` 的 CORE_IDS。
7. **重建与验证**：`npm run build:strategy` →（动 frozen 文件）`node dev/check-frozen-core.js --baseline` → `verify:m4` + `verify:golden` + 全量链。

**自测探针（node）**：对新 family 绑定的每个 KP，跨全部 qt（apply/calc/oral/fill/choice/judge）× 3 难度 × 5 题，断言 ① 生成非空 ② `source==='priority'` ③ `record.id==='generator:<name>'`；再做正确答案回放（标准答案 → 批改全对）。

---

## 12. SEO 与发布运维

### 12.1 可发现性资源

| 资源 | 作用 |
|---|---|
| [robots.txt](file:///Users/zhanggaozhang/Code/Homework%20Help/robots.txt) | 全站允许抓取；显式允许主流 AI 爬虫（GPTBot/OAI-SearchBot/PerplexityBot/ClaudeBot/Googlebot 等）+ Sitemap 指令 |
| [llms.txt](file:///Users/zhanggaozhang/Code/Homework%20Help/llms.txt) | 站点简介 + 核心页面 + 知识库入口（Markdown，供 LLM 读取） |
| sitemap.xml | 站点 URL 清单（首页/FAQ/题型页/knowledge 知识点页），`scripts/generate-sitemap.js` 生成 |
| JSON-LD | index.html（WebSite+Organization）、faq.html（FAQPage）、math-types.html（CollectionPage）、knowledge 页（LearningResource）；`scripts/inject-schema.js` 注入 |
| knowledge/*.html | 400+ 知识点 SEO 详情页，`scripts/generate-knowledge-pages.js` 按源数据哈希增量生成（带 `kbgen:hash` 标记） |

再生成命令：`npm run generate:knowledge`（增量）/ `npm run watch:knowledge`（监听）、`node scripts/inject-schema.js`、`node scripts/generate-sitemap.js`。

### 12.2 版本与缓存发布

- [shared/version.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/version.js)：`APP_VERSION` 单一版本源 → `CACHE_VERSION = 'homework-help-' + APP_VERSION`；SW 与页面共用，`npm run check:sw-version` 校验同步。
- 发布流程：改版本号 → 全量 Gate（§9.1）→ 部署静态文件 → SW 下次激活时按新 CACHE_VERSION 清理旧缓存。
- 上线复核：`curl -s -o /dev/null -w '%{http_code}' https://<域名>/sitemap.xml`（期望 200）；JSON-LD 用 Rich Results Test 校验。

---

## 13. 版本演进与 V2.1 变更明细

### 13.1 引擎版本史

| 版本 | 里程碑 | 关键结果 |
|---|---|---|
| V1.x | 插件轨道（plugins/*.html 模板出题）→ 多 Generator 语义路由体系建立 | Shape 69→104 KP；算术 KP 不再错接图形 |
| V2.0a | Counting/Reasoning/Stats/Picture-Equation + C1/C2 + C5-C6/C7/C9 竞赛生成器 | app-word 342→98；549 KP 全覆盖；竞赛题全部脱离应用题兜底 |
| V2.0b | **MATH-14 方案 B：legacy 插件轨道退役 + 渲染层重构** | 删 ~95 死文件；LegacyAdapter→RenderFormat；native-only 单轨 |
| **V2.1** | **math-g2-column 答案/check 归一化挂账清零 + 引擎版本标签 + 文档体系收敛** | 6 孤儿竖式 KP 正确绑定；有余数除法 native 结构；单源归一化；Golden 15/15 错误答案 0 |

> 应用侧版本（PWA / APP_VERSION）演进流水见 [docs/DEV_LOG.md](file:///Users/zhanggaozhang/Code/Homework%20Help/docs/DEV_LOG.md)（V2.0 → V4.3.0）。

### 13.2 V2.1 变更明细

**问题**：legacy 插件 math-g2-column 覆盖的 8 个 G2-M2 竖式 KP 中，6 个无 native 显式绑定，按题型优先级回退到语义错误的生成器；Golden 自测 4/5 但被 WARNING 放行。

| KP | 修复前路由 | 修复后 |
|---|---|---|
| `math-g2-m2-add-col` | arithmetic-addition（隐式回退） | arithmetic-addition（显式绑定） |
| `math-g2-m2-sub-col` | **arithmetic-addition（减法→加法，错）** | arithmetic-subtraction |
| `math-g2-m2-remainder-col` | **application-word（生成应用题，错）** | arithmetic-division（div-remainder 结构） |
| `math-g2-m2-chain-add-col` | **application/position（错）** | complex-calc（chain，[+]，2 步） |
| `math-g2-m2-chain-sub-col` | **application/position（错）** | complex-calc（chain，[−]，2 步） |
| `math-g2-m2-mixed-col` | **application/position（错）** | complex-calc（chain，[+,−]，2 步） |

验证：8 KP × d=1..10 × 20 题 = **800/800 正确，错误答案 0**；余数记号变体（`……/…/.../余`）全判对；浏览器实测无 JS 错误、无 404。

---

## 14. 踩坑经验与教训

| # | 坑 | 现象 | 教训 / 对策 |
|---|---|---|---|
| P1 | **operations 字段泛型污染** | 竞赛/图形 KP 无 operations 字段，算术阻断失效，342 KP 落应用题兜底 | 语义判定不能依赖可选字段；硬阻断 + 显式语义函数双保险 |
| P2 | **recognize 元题型误阻断** | 图形识别 KP 在非 choice/judge 题型被阻断链拒绝 | recognize 不属于任何语义域，硬阻断必须统一豁免 `qt==='recognize'` |
| P3 | **新生成器上线不清旧绑定** | C2 已建新生成器，旧应用生成器仍声明这些 KP，评分争抢 | 每次迁移 KP 必须跑跨生成器冲突扫描；registry 与实例 kps 双处同步 |
| P4 | **双绑定只改一处** | 只改注册表没改生成器实例（或反之），路由不一致 | 绑定 = 注册表 + 实例数组两处；改后用 forKnowledgePoint() 双向验证 |
| P5 | **语义优先级缺失** | 数阵/幻方 pluginId 含 c1 被 C1 抢走（实际应走 picture-equation） | 多 family 重叠时以结构性信号（graphicType=diagram）优先，并写回归断言 |
| P6 | **同文件并行 Edit 静默回退** | 同一条消息里对同一文件多个 Edit，部分改动丢失 | 一次只编辑一个文件的一处，改完立即 grep/read 复核 |
| P7 | **改源忘重建 bundle** | Node 探针验证通过、浏览器行为仍旧 | 改 shared/ 源 → 必须 `npm run build:strategy`；浏览器验证前确认 bundle 含新标识 |
| P8 | **WARNING 放行积累技术债** | golden 自测 4/5 仅 warning，column 归一化问题长期挂账 | 质量红线进 ERROR 阻断；「错误答案 0」不能是警告 |
| P9 | **归一化口径分散** | 余数记号 ……/.../余 在批改/golden/页面各处手写处理，口径漂移 | 归一化单源（normalizeAns），调用方禁止各自 trim/替换 |
| P10 | **孤儿 KP 隐式回退** | 无显式绑定的 KP 按 qt 优先级回退到「能答但语义错」的生成器（减法竖式→加法） | 覆盖扫描必须检查 source='priority' 且语义匹配；不接受「能生成」即通过 |
| P11 | **浏览器测试脚本自身伪影** | DOM 提取题干时正则把题号「4」拼进被除数「7」造成 47÷3 假错题 | 区分生成器 bug 与测试脚本 bug；题卡作用域内先剥离题号再匹配；与标准答案回放交叉验证 |
| P12 | **package.json 引用已删脚本** | legacy 退役后 test 链仍指向已删除脚本导致 Gate 报错 | 删除文件时同步清理 package.json / layers.json / sw.js 预缓存 / lint 白名单 |
| P13 | **G1-G3 无高年级模块误报** | verify-setup 对 G1-G3 强查 M4+ 模块必然失败 | 课程进度是渐进的，M1-M12 结构完整性只对 G4-G6 强制 |
| P14 | **分支判定中文泛词误捕** | 「裂项」匹配到「整数裂项」等兄弟题型 | 分支用稳定 id slug；中文仅用于全文关键词且用完整词 |
| P15 | **文档半过期并存** | DEVELOPMENT/ARCHITECTURE 等文档描述 plugins 双轨时代现状，与 native-only 代码矛盾，误导后来者 | 文档单一事实源（D13）；已落地草案/快照用后即删，现状只保留一份权威文档 |

---

## 15. 文件索引与验证命令手册

### 15.1 核心文件地图

| 层 | 文件 | 职责 |
|---|---|---|
| 知识层 | [shared/knowledge-bank.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/knowledge-bank.js) / knowledge-math.js | 549 数学 KP 只读数据源 |
| 知识层 | [shared/knowledge-point.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/knowledge-point.js) | KP.get() 规范对象、惰性索引、pluginId/moduleId 解析 |
| 关联层 | [shared/practice-bridge.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/practice-bridge.js) | PracticeBridge + ControlService（UI 唯一入口） |
| 策略层 | [shared/strategy/strategy-engine.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/strategy/strategy-engine.js) | 难度→QuestionPlan、语义约束注入 |
| 路由层 | [shared/generator/generator-selector.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generator-selector.js) | 评分 + 硬阻断链（**frozen**） |
| 路由层 | [shared/generator/generator-registry.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generator-registry.js) | CORE_RECORDS 绑定表（**frozen**） |
| 生成层 | [shared/generator/generators/](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/generators) | 23 个 Core Generator；index.js 含 ENGINE_VERSION |
| 生成层 | [shared/generator/core/arithmetic-core.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/core/arithmetic-core.js) | 算术结构构造器（15 SPECIAL_KINDS + rng.js） |
| 生成层 | [shared/generator/core/kp-arithmetic-semantics.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/generator/core/kp-arithmetic-semantics.js) / kp-complex-semantics.js | 语义解析 |
| 表现层 | [shared/presentation/render-format.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/presentation/render-format.js) | SemanticQuestion → 可渲染格式（唯一轨） |
| 表现层 | [shared/presentation/svg-registry.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/presentation/svg-registry.js) | 图形描述符 → SVG 插件分派 |
| 批改层 | [shared/core.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/core.js) / [shared/check.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/check.js) | normalizeAns 单源归一 / defaultQCheck |
| 页面层 | [practice.html](file:///Users/zhanggaozhang/Code/Homework%20Help/practice.html) / select.html / index.html | 练习页 / 三维选择 / 首页（UI 设计见《设计文档》） |
| 构建 | dev/build-strategy-bundle.js → shared/strategy-engine.bundle.js（70 modules） |
| 门禁 | dev/check-frozen-core.js / check-golden.js / check-generator-registry.js / check-generator-contract.js / check-architecture-layers.js / check-architecture-rules.js / lint-check.js |
| 测试 | tests/generator/*.test.js、tests/strategy/*.test.js、test/、test/unit/ |
| 文档 | [技术文档--基础.md](file:///Users/zhanggaozhang/Code/Homework%20Help/技术文档--基础.md)（本文）、[设计文档.md](file:///Users/zhanggaozhang/Code/Homework%20Help/设计文档.md)、[docs/DEV_LOG.md](file:///Users/zhanggaozhang/Code/Homework%20Help/docs/DEV_LOG.md) |

### 15.2 常用命令

```bash
# 改 shared/ 源文件后
npm run build:strategy                              # 重建 strategy bundle
node dev/check-frozen-core.js --baseline            # 重建 frozen 基线（动了 frozen 文件时）
node dev/check-frozen-core.js --check               # 校验 frozen

# 全量 Gate（交付前）
npm run verify:m4 && npm test && npm run verify:golden && npm run verify:syntax && \
npm run verify && npm run verify:m2 && npm run verify:layers && npm run verify:frozen-core

# 单元测试
node --test test/*.test.js test/unit/*.test.js tests/generator/*.test.js tests/strategy/*.test.js

# 知识库 / SEO 产物
npm run verify:kb-change                            # KP 修改最小门禁
npm run generate:knowledge                          # 增量再生成 knowledge/ 静态页
node scripts/generate-sitemap.js                   # 再生成 sitemap.xml
```

---

> **文档状态**：V2.1 基础技术文档；引擎版本标签 `ENGINE_VERSION=2.1.0` 位于 shared/generator/generators/index.js。
> **核心不变，边界可扩。稳定是最大的功能。**
