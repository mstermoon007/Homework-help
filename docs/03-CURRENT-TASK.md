# 03-CURRENT-TASK — 当前任务

> 状态：ACTIVE
> 本文档回答：现在唯一应该做什么。

## 当前状态

T10 最终冻结已完成（2026-09-19），项目进入**维护模式**。
没有进行中的开发任务；禁止在无任务书的情况下开始编码。

## 冻结期唯一允许的工作方式

任何新任务开始前必须先回答八问：

```text
① 当前时间轴在哪一阶段？      → T10 冻结后维护期
② 当前阶段的唯一目标是什么？  → （新任务填写，禁止"优化/完善/重构"类模糊任务）
③ 哪些内容已经 FROZEN？       → 见 01-ARCHITECTURE-FROZEN.md
④ 本任务修改哪一层？
⑤ 是否存在重复实现？
⑥ 是否可以删除而不是继续兼容？
⑦ 如何证明修改正确？          → bash scripts/run-all-checks.sh 全绿 + 定向验证
⑧ 如何证明没有扩大范围？
```

## 新任务书最小结构（强制）

```text
# 任务名称
## 1. 当前状态        ## 2. 时间轴位置
## 3. 当前基线        ## 4. 任务目标
## 5. 不修改项        ## 6. Step 01..05（定位/判断/最小修改/定向验证/全量验证）
## 7. PASS / FAIL / BLOCKED
## 8. 实际修改文件    ## 9. 下一阶段
## 10. 文档状态
```

## 修改后的强制动作

```text
定位 → 审计 → 判定 → 最小修改 → 定向验证 → 全量验证（bash scripts/run-all-checks.sh）→ 记录 → 冻结
```

## 遗留问题处理入口

仅当用户明确要求处理时才启动，且必须重跑全量回归：

1. capacity-map.json 重建：`node dev/scan-capacity.js --refresh` + 全量回归（会改变 typeCaps 行为）。
2. 其余见 `00-PROJECT-BASELINE.md` §6。

## 文档维护规则

- 完成一个阶段 → 更新 `02-DEVELOPMENT-TIMELINE.md` 对应条目并覆盖本文件。
- 历史文档一律进 `archive/`，不得作为当前开发依据。
