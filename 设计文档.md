# 数学题目与练习生成引擎 — 设计文档

> **引擎版本**: V2.1（v2.1.0）
> **文档定位**: 设计目标与原则、已落地设计计划的决策记录、**UI 层设计说明**。架构实现与门禁细节见 [技术文档--基础.md](file:///Users/zhanggaozhang/Code/Homework%20Help/技术文档--基础.md)
> **更新时间**: 2026-09-08（由原 DRIVEN_GENERATION_PLAN / SPIRAL_IMPROVEMENT_PLAN / PRINT_COMPACT_PLAN 等已落地设计计划蒸馏而成，原文件已删除）

## 目录

1. [设计目标与原则](#1-设计目标与原则)
2. [已落地设计计划（决策记录）](#2-已落地设计计划决策记录)
3. [UI 层设计：页面地图与用户流程](#3-ui-层设计页面地图与用户流程)
4. [UI 层设计：练习页（practice.html）](#4-ui-层设计练习页practicehtml)
5. [UI 层设计：视觉令牌与样式体系](#5-ui-层设计视觉令牌与样式体系)
6. [UI 层设计：打印排版](#6-ui-层设计打印排版)
7. [UI 层设计：离线缓存与可发现性](#7-ui-层设计离线缓存与可发现性)
8. [预留设计：自适应难度](#8-预留设计自适应难度)
9. [UI 改动约束清单](#9-ui-改动约束清单)

---

## 1. 设计目标与原则

| 原则 | 含义 |
|---|---|
| **知识点驱动** | 一切出题请求以知识点（KP）为中心：单 KP 练习、多 KP 配额练习、综合练习按 KP weight 加权采样；「每知识点 N 题」是产品承诺，由关联层 ControlService 编排兑现 |
| **螺旋上升** | 同一主题跨年级螺旋复现（slug 跨年级一致）；难度（1–10）、深度（spiral S1–S6 + 前置链）、自适应（预留）三维递进 |
| **教材同步** | 题型与人教版教材、主流练习册对齐；基础模块 M0–M12 + 竞赛模块 C1–C9（4–6 年级） |
| **纯前端零依赖** | 无服务器、无注册、无数据上传；crypto 随机源；经典 `<script>` 加载；Service Worker 离线 |
| **家长友好** | 一键出题、即时批改、答案显隐、自动计时、错题重做、A4 纯净打印；无广告无弹窗 |
| **正确优先** | 「错误答案 0」是硬红线：每道题标准答案必须通过自身批改（Golden 自测），失败即阻断发布 |

---

## 2. 已落地设计计划（决策记录）

以下设计计划均已实施并由门禁固化，此处保留**设计决策与落地状态**作为背景，原始执行步骤文档已删除。

### 2.1 受控生成链路（知识点驱动 × 配额编排）

- **问题**：快速/教师模式下「题型/知识点选择 → 生成页 → 按知识点出对应数量题目」链路存在断点：每-KP 配额（kpAllocation）在生成层被忽略，编排逻辑散落。
- **方案**：
  - 生成层只认 `count/difficulty/knowledgePointId(s)/questionType`；**题量配额下沉**到 generation-engine 的 multi-kp 分支按配额规划。
  - 关联层 `PracticeBridge.control`（ControlService·服务模式）集中解析参数，产出两类执行计划：`single`（单次直发）与 `orchestrated`（多 KP 带配额时并发调生成层 `start()`、合并为一个题目集、聚合会话统一批改）——**不改生成层即兑现「每知识点数量」**。
  - 清理死路径：`runOrchestrated`/`aggregateSession`/`kpAllocationPartitions` 等编排冗余移除；`kpAllocation` 死字段转为透传。
- **落地状态**：✅ 已落地；`dev/check-ui-boundary.js` 锚定 UI 唯一生成入口。

### 2.2 三维螺旋（难度 × 深度 × 自适应）

- **方案**：
  - **难度维**：年级难度锚点表（G1 锚 1-2 … G6 锚 6-10），用户 1–10 输入经 clamp 与结构五档映射。
  - **深度维**：spiral-strategy 变体档 S1–S6 与难度结构档联动（S1↔难度 1-2 … S6↔9-10）；深度度量 = `complexityScore(difficulty) + spiral_level + 前置链深度`，随年级严格递增；前置关系来自 KP 的 prerequisites DAG。
  - **自适应维**：adaptive-strategy 实现但**默认关闭**（见 §8）。
- **落地状态**：✅ 难度/深度已落库并由 `verify:difficulty-anchor`、`check-spiral-consistency` 门禁守护；自适应契约固化待接。

### 2.3 打印排版紧凑化

- **问题**：屏幕题卡样式直接打印导致纸张浪费；打印存在两条样式路径（print.js PRINT_QCSS 与 DOM 克隆打印）且已漂移。
- **方案**：
  - 档一（必做）：打印**去卡片化**——去除卡片边框/阴影/大间距，题号去圆形徽标，作答区保留横线；PRINT_QCSS 与旧路径样式统一；令牌收敛到 tokens.css。
  - 档二：`density` 密度参数真正生效——renderer 透传 → html-renderer 输出 `compact` 类 → components.css `.question-card.compact` 校准。
  - 打印时隐藏全部按钮/工具栏/无关元素，预览与打印共用同一 HTML。
- **落地状态**：✅ 已落地（`shared/print.js` PRINT_ROUTES/PRINT_QCSS + components.css compact）。

### 2.4 R2 题型规范化

- **方案**：G2–G6 非 canonical 题型改写为 canonical 题型（oral/calc/fill/choice/judge/apply/recognize/geometry/open），原始题型保留在 `rawType` 审计字段；五类本体（concept/factual/graphic/common_errors/operation）按批次补齐。
- **落地状态**：✅ 已落地；`check-type-module-consistency.js` 门禁守护题型/模块一致性；本体经 M1 系列门禁（schema/operation/factual/error）校验。

### 2.5 legacy 插件轨道退役（MATH-14 方案 B）

- **问题**：native 生成器与 88 个 legacy 插件双轨并存，migration-switch 使 549 KP 的路由事实无法在 native 侧单点验证；死代码持续污染门禁与 bundle。
- **方案**：LegacyAdapter 渲染路径收口为 RenderFormat 单轨；删除 ~95 个死文件（plugins 模板插件、legacy-adapter、plugin-loader、GenCap、migration-switch、legacy-svg-adapter、死工具脚本）；practice.html / practice-bridge.js / core.js / sw.js / layers.json / lint / 测试全部去 legacy 化；**保留** 6 个教学图形 SVG 插件（clock/area/fraction/data-stats/draw/competition）经 SVGRegistry 分派。
- **落地状态**：✅ 已落地；native-only 单轨，`generator-mode.js` 默认 native 无 legacy fallback。

### 2.6 math-g2-column 归一化挂账清零（V2.1）

- 见 [技术文档--基础.md §13.2](file:///Users/zhanggaozhang/Code/Homework%20Help/技术文档--基础.md)：6 个孤儿竖式 KP 显式绑定、有余数除法 native 结构、`normalizeAns` 单源归一、Golden 失败升级为 ERROR。

---

## 3. UI 层设计：页面地图与用户流程

### 3.1 页面地图

| 页面 | 角色 | 设计要点 |
|---|---|---|
| [index.html](file:///Users/zhanggaozhang/Code/Homework%20Help/index.html)（1524 行） | 首页/营销落地页 | 双海报展示、科目+年级选择、「一键开始」CTA、问题反馈入口；WebSite+Organization JSON-LD；移动端优先响应式 |
| [select.html](file:///Users/zhanggaozhang/Code/Homework%20Help/select.html)（992 行） | **统一三维选择页** | 科目 / 年级 / 知识点（按模块 M0–M12、C1–C9 分组）三级选择；题量与难度预设；选中后跳转 practice.html 并透传 query |
| [practice.html](file:///Users/zhanggaozhang/Code/Homework%20Help/practice.html)（1778 行） | **核心练习页** | 生成/作答/批改/打印/错题本/计时统一宿主（详见 §4） |
| subject-types.html / math-types.html | 转发桩 | 302 式 `location.replace` 到 select.html（math-types 透传 `subject=math`），保留旧链接与 CollectionPage JSON-LD |
| [faq.html](file:///Users/zhanggaozhang/Code/Homework%20Help/faq.html) | 常见问题 | FAQPage JSON-LD |
| [contact.html](file:///Users/zhanggaozhang/Code/Homework%20Help/contact.html) | 问题反馈 | 反馈表单入口 |
| knowledge/*.html（400+） | SEO 知识点详情页 | 由 `scripts/generate-knowledge-pages.js` 从知识库哈希增量生成，LearningResource JSON-LD，带 `kbgen:hash` 标记 |
| sw.js | 离线缓存宿主 | 经典 Worker + importScripts(version.js)，按 CACHE_VERSION 预缓存与清理 |

### 3.2 主用户流程

```
index.html                选科目（数学）+ 年级 → 一键开始
   │
   ▼
select.html               选知识点（可多选/全模块）→ 题量（20/30/50/自定义）
   │                      → 难度（1–10，默认低档）→ 开始练习
   │  （URL query 透传：subject/grade/kp/kps/count/difficulty）
   ▼
practice.html             生成练习（进度乐观 UI）→ 孩子作答（自动计时）
   │                      → 检查答案（逐题对错 + 得分/正确率/鼓励语）
   │                      → 错题重做 / 显示答案 / 打印页面 / 重新生成 / 刷新换题
   ▼
（闭环）                   Toast 引导下一步；结果回灌 learner（掌握度 EMA，本地存储）
```

深链支持：practice.html 直接接收 URL 参数即可出题（保旧版 loopToast 链接），无参数时进入页内「装配区」就地配置。

---

## 4. UI 层设计：练习页（practice.html）

### 4.1 区域结构

| 区域 | 实现 | 职责 |
|---|---|---|
| 配置面板 | `<section class="panel controls" id="controlsPanel">` | 题量 chips（**20题 / 30题 / 50题 / 自定义**）、难度（1–10 range，提示「1~10」）、**知识点占比**（教师模式：每知识点数量填空）、当前知识点面包屑（`📖 当前知识点`） |
| 题卡宿主 | `<section id="problemsArea">` | 渲染层产出的成品题卡 HTML 直接注入；页面不拼题卡；输入框统一 `input[data-index]` / `data-idx` 选择器；列数自适应 |
| 结果面板 | `<section class="result-panel" id="resultArea" aria-live="polite" aria-atomic="true">` | 批改结果：逐题对错标记、得分、正确率、鼓励语；**aria-live** 供读屏器播报 |
| 工具栏按钮 | 生成练习 / 检查答案 / 显示答案 / 打印页面 / 重新生成 / 刷新 / 错题重做（N） | 打印时全部隐藏 |
| 反馈组件 | 全局 Toast（含「知道了」闭环引导）、全局错误捕获、生成进度乐观 UI、异常时「重新加载页面」兜底 |

### 4.2 交互设计要点

- **自动计时**：题目生成后开始计时，提交答案时停止，帮助家长掌握做题速度。
- **答案显隐**：可随时显示/隐藏标准答案，支持「题卡模式」（只出题不做）。
- **错题重做**：批改后按错题数生成「错题重做（N）」入口，针对错题知识点重新出题。
- **乐观 UI**：点击生成后立即进入进度态，失败有明确兜底（错误捕获 + 重载引导），不出现白屏。
- **多空作答**：有余数除法等多空题（商/余数）按 `data-index` 逐空收集，批改逐空判定。

### 4.3 交互边界（硬约束）

- UI **只调** `PracticeBridge.start / submit / newSession / onStartFeedback / onSubmitFeedback`；不直接 `new PracticeSession()`、不直接调生成引擎内部。
- 题卡 HTML 来自引擎成品（`state.lastSemantic.html`）；页面只负责注入、答案收集（遍历 `#problemsArea input[data-index]`）、批改标记（`.question-card[data-index]` 状态类）、打印。
- 门禁锚定：`dev/check-ui-boundary.js`（唯一生成入口 = GenerationEngine.generate / PracticeBridge.start）、`dev/check-practice-page.js`（practice.html 职责 9/9）。

---

## 5. UI 层设计：视觉令牌与样式体系

### 5.1 分层样式（@layer 锁序）

```
tokens → base → components → states → toolbar → pages → subjects
```

| 文件 | 职责 |
|---|---|
| tokens.css | **唯一设计令牌源**：品牌色 `--brand`、墨色 `--ink`、弱化 `--muted`、线 `--line`、对错 `--ok/--bad`；科目三色 `--math/cn/en-primary/secondary/accent`；圆角/阴影/渐变 |
| base.css | 全局重置、排版基线 |
| components.css | 共享组件：题目卡片（含 `.compact` 紧凑态）、按钮、chip、题型卡片、面板 |
| states.css | 状态样式（对错标记、禁用、高亮） |
| toolbar.css | 练习页工具栏 |
| pages.css | 页面级布局（首页/选择页/练习页） |
| subjects.css | 科目差异化主题 |

### 5.2 设计规则

- 颜色一律 `var(--*)`；禁止硬编码色值（lint R2），SVG `fill/stroke` 不支持 var() 属例外。
- 移动端优先：题卡列数随宽度自适应；触控目标尺寸友好。
- 教学图形：引擎输出图形描述符 → SVGRegistry 分派（时钟、面积模型、分数、统计图表、作图、竞赛图 + 几何/竖式/凑十基础 SVG），**不在页面内画 SVG**。

---

## 6. UI 层设计：打印排版

- **目标**：一键生成 A4 友好练习卷——去卡片化（无边框/阴影/大间距）、紧凑密度、作答区留横线、按钮工具栏全隐藏。
- **实现**：[shared/print.js](file:///Users/zhanggaozhang/Code/Homework%20Help/shared/print.js) 的 PRINT_ROUTES 打印路由 + PRINT_QCSS 打印专用样式；预览与打印共用同一 HTML；`density` 密度经 renderer → html-renderer `compact` 类生效。
- **约束**：打印样式改动属 Frozen Core（M7），需按 Bug Fix 授权流程走（见技术文档 §8.4）。

---

## 7. UI 层设计：离线缓存与可发现性

### 7.1 离线（PWA）

- `sw.js` 经典 Worker，`importScripts('shared/version.js')` 读取 APP_VERSION；`CACHE_VERSION = 'homework-help-' + <APP_VERSION>`。
- 预缓存全部页面与 shared/ 资源；版本号变更触发 SW activate 并清理旧缓存。
- `scripts/sync-sw-version.js`（`npm run check:sw-version`）保证 SW 缓存名与 version.js 同步。

### 7.2 可发现性（SEO / LLM 友好）

- **robots.txt**：全站 Allow；显式允许 GPTBot / OAI-SearchBot / PerplexityBot / ClaudeBot / Googlebot 等 AI 与搜索爬虫；声明 Sitemap。
- **llms.txt**：Markdown 站点简介 + 核心页面 + 知识点入口，供 LLM 直接读取。
- **sitemap.xml**：`scripts/generate-sitemap.js` 生成（首页/FAQ/选择页/knowledge 知识点页）。
- **JSON-LD**：WebSite+Organization（首页）、FAQPage（faq）、CollectionPage（math-types）、LearningResource（knowledge 页）；`scripts/inject-schema.js` 注入。
- **知识点静态页**：knowledge/*.html 由知识库数据增量生成，承载长尾搜索（「二年级 有余数除法 练习」等）。

---

## 8. 预留设计：自适应难度

已实现、**默认关闭**的预留契约（固化，不做新开发；接入时不破坏现有链路）：

| 字段 | 含义 |
|---|---|
| `request.adaptive` | 自适应开关（practice-bridge 透传，ControlService.plan 保留字段） |
| `learnerProfile` | 学习者状态：最近正确率、连续对/错、KP 级掌握度（learner/ 模型已产出） |
| `adaptiveDelta` | 难度调整量（−2..+2），叠加在用户难度上，target-difficulty 已 clamp 1–10 |

接入点（strategy-request / practice-bridge / target-difficulty）均已识别字段，未消费时安全忽略。当前线上难度 = 用户选择 + 年级锚点；自适应开启后 = 用户难度 + EMA 反馈（`ema=ema*0.6+rate*0.4`）+ delta 规则。

---

## 9. UI 改动约束清单

改动 UI 层时必须遵守：

1. **不新增生成逻辑**：题目结构/答案/批改一律来自引擎；UI 不拼题卡、不判答案（收集与展示除外）。
2. **唯一入口**：所有出题/提交经 PracticeBridge 公开方法；改后跑 `npm run verify:ui-boundary` 与 `verify:practice-page`。
3. **样式走令牌**：新颜色/圆角/阴影先入 tokens.css，不硬编码；打印态同步检查 PRINT_QCSS。
4. **透传参数不断链**：select.html → practice.html 的 query（subject/grade/kp/kps/count/difficulty/print）字段增删需两端同步。
5. **转发桩保留**：subject-types.html / math-types.html 为旧链接保活，不得删除（sitemap 与旧入口依赖）。
6. **可访问性**：结果区保持 `aria-live="polite"`；表单控件有 label；触控目标不小于令牌规定尺寸。
7. **SW 预缓存同步**：新增页面/共享资源需同步 sw.js precache 清单并跑 `check:sw-version`。
8. **SEO 产物再生成**：改知识点/页面结构后 `npm run generate:knowledge` + `node scripts/generate-sitemap.js`。

---

> 设计文档与 [技术文档--基础.md](file:///Users/zhanggaozhang/Code/Homework%20Help/技术文档--基础.md) 配套阅读：本文档回答「为什么这样设计、UI 怎么组织」，技术文档回答「怎么实现、怎么验证」。
